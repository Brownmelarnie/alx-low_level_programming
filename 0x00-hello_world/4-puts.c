#include <stdio.h>
/**
 * main - A program that prints a line with puts function n
 * Return: Always 0(Success)
 */
int  main(void)
{
puts("\"programming is like building a multilingual puzzle");
return (0);
}
